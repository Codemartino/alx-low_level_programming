#include "main.h"
/**
 * print_alphabet - prints the alpa  in lowercase
 *
 * Return: Always 0
 */
void print_alpabet(void)
{
	char la;

	for (la = 'a'; la <= 'z'; la++)
		_putchar(la);

	_putchar('\n');

}
