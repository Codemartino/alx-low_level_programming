#include "main.h"

/**
  * _isdigit - Check if a character is digit
  * @c: Character to check
  *This checks for if a character is digit
  * Return: 1 if c is a digit, otherwise 0
  */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
