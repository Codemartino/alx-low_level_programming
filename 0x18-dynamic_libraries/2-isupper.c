#include "main.h"

/**
 * _isupper - check if character is uppercase
 * @c: character to check
 *
 * Checks if a character us uppercase
 * Return: 1 if c is uppercase, 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
